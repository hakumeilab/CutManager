# Changelog

## Unreleased

## 0.3.1 - 2026-06-16

### 追加した機能

- `区分` `TP入れ回数` `BG入れ回数` は、V 字アイコン部分の 1 クリックで候補を開けるように変更
- `TP入れ日` `BG入れ日` `納品日` にカレンダーアイコンを表示し、アイコン部分の 1 クリックでカレンダー入力を開けるように変更
- カット番号の右に `メモ` 列を追加し、リボン右側にメモ欄を追加
- 列ヘッダーをドラッグして表示順を変更できるようにし、表示順をローカル設定に保存するように変更
- 通常の行追加位置を、選択セルの下ではなく上に変更
- リボン右側のメモ欄を話数全体メモとして扱うように変更
- アプリ本体、配布 exe、インストーラーに `assets/cutmanager_icon.ico` のアプリアイコンを設定

### バグ修正

- リボン開閉ボタンをリボン本体から分離し、本文だけを開閉する挙動へ変更
- リボン開閉ボタンと候補セルの V 字アイコンを小さく控えめな色へ調整
- リボン開閉ボタンを標準ボタン描画から自前描画へ変更し、閉じる瞬間に V 字が消えるちらつきを抑制
- リボン本文を固定サイズのままクリップ領域内でスライドさせ、開閉時に各セル枠が潰れるような重い動きを軽減
- リボン開閉を即時切り替えに変更し、開閉中に表全体のレイアウトが毎フレーム更新されないよう調整
- 表の行色と特殊セル色の計算をキャッシュし、ウィンドウサイズ変更時の再描画負荷を軽減
- 表の色キャッシュを行単位中心に見直し、テーマ適用時の不要な viewport 更新を削減
- リボンの V 字行を上側に固定し、開閉時に位置が揺れにくいよう調整

## 0.3.0 - 2026-06-16

- `素材入れ回数` `素材入れ日` を `TP入れ回数` `TP入れ日` に変更し、旧 CSV からの読み替えに対応
- `BG入れ回数` `BG入れ日` を追加し、PSD/PSB ファイルのドロップを BG 入れとして反映
- 画面上部に総カット数、残り TP カット数、残り BG カット数、残り納品数、兼用カット数、BANK 数を表示するリボンバーを追加
- リボンバーを開閉できるようにし、納品、TP、BG の 3x3 進捗表示へ変更
- `TP入れ回数` の `BGOnly`、`BG入れ回数` の `全セル` を候補入力できるようにし、不要作業として残数から除外
- `TP入れ日` `BG入れ日` `納品日` をダブルクリックしたときにカレンダーで入力できるように変更
- リボンに欠番数を追加し、兼用、BANK、欠番の表示色を表の区分色に合わせるよう調整

## 0.2.9 - 2026-04-16

- カットフォルダー単体をドロップしたときに、その中に子フォルダーがあってもフォルダー自身のカット名を優先して取り込むよう修正
- 動画ドロップ時の未一致件数を抽出カット数ではなく未一致ファイル数で集計するよう修正
- onefile exe の自動更新で自己置換ロジックが実行されない不具合を修正
- zip 配布の自動更新で削除済みファイルが残留しないよう、展開先を同期する更新処理に修正
- 更新処理、動画取り込み、フォルダー取り込みの回帰テストを追加

## 0.2.8 - 2026-04-14

- ダークテーマとライトテーマの切り替え時に、表、ヘッダー、メニューバーの配色が崩れないよう修正
- 交互行と `兼用` `BANK` `欠番` の行色を、テーマに応じて見やすい配色に調整
- `区分` を変更したときに、行全体の色が確実に反映されるよう描画処理を修正
- ドロップヒントを表の上にオーバーレイ表示し、表の外周に余白が出ないよう修正
- セル選択表示と通常セル編集時の余白を見直し、表の見た目を整理

## 0.2.7 - 2026-04-13
- 空の CSV に動画ファイルをドロップしたとき、確認後に動画名からカットを仮登録して納品情報を反映できるように変更
- 既存 CSV で一致しない動画があるとき、未一致ファイル名を一覧表示し、必要なら未一致分だけ仮登録できるように変更
- README の構成を見直し、ユーザー向けの使い方を前半、セットアップやビルド手順を後半へ移動
- README の最新版ダウンロード手順に単体版 `windows-onefile.exe` とインストーラー版 `windows-setup.exe` の違いを追記

## 0.2.6 - 2026-04-03

- Release asset に Inno Setup の `windows-setup.exe` を追加
- アプリ内更新では onefile exe の自己上書きではなく、setup exe の起動を優先するように変更
- インストール先の既定値を `%LOCALAPPDATA%\Programs\CutManager` に設定

## 0.2.5 - 2026-04-03

- Release workflow の GitHub Release 作成/更新 step が、初回作成時でも落ちないように修正

## 0.2.4 - 2026-04-03

- `aaa_01_001_B1.mov` のような動画ファイル名から `テイク=B` `テイク番号=1` を抽出できるように変更
- `take02` `tk03` `t04` 形式は引き続き `テイク=T` として反映

## 0.2.3 - 2026-04-02

- 配布形式を onefile の単体 exe に切り替え
- README の冒頭に最新版 exe を Release からダウンロードする手順を追加
- アプリ内更新で onefile の exe をダウンロードして自己置換できるように変更

## 0.2.2 - 2026-04-02

- Nuitka 配布版でも zip 更新の自動適用を利用できるように更新実行環境の判定を修正
- zip 更新が使えない場合のメッセージを実行ファイル名に依存しない形へ調整

## 0.2.1 - 2026-04-02

- `区分` 列の値に応じて行全体の背景色を表示するように変更
- `欠番` は濃いグレー、`兼用` は薄い緑、`BANK` は薄い赤で表示

## 0.2.0 - 2026-04-02

- GitHub Releases を参照するアプリ内更新チェックを追加
- リリース asset のダウンロードと Windows 向け更新適用を追加
- GitHub Releases を使った配布手順を README に整理
- `build_release.ps1` による配布 zip と SHA-256 の生成手順を追加

## 0.1.0 - 2026-04-01

- CSV 編集 GUI の初回リリース
- 素材フォルダーと動画ファイルのドラッグアンドドロップ取り込みを実装
- 並べ替え、絞り込み、アンドゥ/リドゥ、最近使ったファイルを実装
